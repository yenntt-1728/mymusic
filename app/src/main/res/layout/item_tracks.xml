<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="com.example.mymusic.model.Track" />
        <variable
            name="itemClickListener"
            type="com.example.mymusic.adapter.TracksAdapter.OnItemClickListener" />
        <variable
            name="listener"
            type="com.example.mymusic.screen.track.ItemTrackViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="6dp">

        <LinearLayout
            android:id="@+id/linear_item_track"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:orientation="horizontal">
<!--            android:onClick="@{listener::onItemTrackClick}"-->


            <ImageView
                android:id="@+id/image_view"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:scaleType="center"
                android:background="@color/colorPrimaryDark"
                android:src="@drawable/ic_music_note_black_24dp"
                />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="70dp"
                android:layout_marginLeft="6dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/text_view_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLength="30"
                    android:lines="2"
                    android:textSize="18sp"
                    android:scrollHorizontally="true"
                    android:singleLine="true"
                    android:textColor="@color/colorWhite"
                    android:text="@{viewModel.title}" />

                <TextView
                    android:id="@+id/text_view_album_count"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:text="@{viewModel.artist}"
                    android:textColor="@color/colorWhite"
                    android:lines="1"/>
            </LinearLayout>
        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>
